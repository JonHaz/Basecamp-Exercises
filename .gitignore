__pycache__/
day1/04_diagnosing-ai-problems/runs.jsonl

# Secrets — your API key lives in a gitignored .env (never committed).
# A bare ".env" with no slash ignores it in every folder, root or per-exercise.
.env
key.env
*.local

# Local environments / cruft
.venv/
.ipynb_checkpoints/

# Eval harness output — the executed notebook cells are the evidence, not these blobs
day2/01_evals/eval_results/

# day2/02 — frozen v0 baseline; the executed cell output is the evidence, not the JSON
day2/02_inference-optimization/baseline_v0.json
day2/02_inference-optimization/run_cache/
